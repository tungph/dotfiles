#!/usr/bin/env bash

echo "> $1"
CMD="$HOME/.dotfiles/bin/installers/$1.sh"
if [ -f "$CMD" ]; then
    echo "Running $CMD"
    . "$CMD" 2>&1 >/dev/null
else
    sudo apt install -y "$1"
fi

echo ""
exit 0
