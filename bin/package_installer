#!/usr/bin/env bash

echo "> $1"
CMD="$HOME/.dotfiles/bin/installers/$1.sh"
if [ -f "$CMD" ]; then
    echo "Running $CMD"
    . "$CMD" >/dev/null
else
    sudo apt install -y "$1" >/dev/null
fi

echo ""
exit 0
